<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tip Calculator</title>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap');

    body {
        font-family: 'Montserrat', sans-serif;
        background: linear-gradient(135deg, #ff758c, #ff7eb3);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        color: #fff;
    }

    .calculator {
        background: rgba(0,0,0,0.25);
        padding: 25px 30px;
        border-radius: 20px;
        box-shadow: 0 8px 20px rgba(0,0,0,0.4);
        width: 280px;
        text-align: center;
    }

    .calculator h1 {
        margin-bottom: 15px;
        font-size: 24px;
        font-weight: 600;
    }

    label {
        display: block;
        text-align: left;
        font-size: 14px;
        margin-top: 10px;
        font-weight: 500;
    }

    input[type="number"] {
        width: 100%;
        padding: 10px;
        margin-top: 5px;
        border-radius: 10px;
        border: none;
        font-size: 16px;
    }

    button {
        width: 100%;
        padding: 12px;
        margin-top: 15px;
        background: #1f4037;
        background: linear-gradient(to right, #1f4037, #99f2c8);
        border: none;
        border-radius: 10px;
        color: #fff;
        font-size: 17px;
        font-weight: 600;
        cursor: pointer;
        transition: 0.3s;
    }

    button:hover {
        opacity: 0.85;
    }

    #result {
        margin-top: 18px;
        background: rgba(255,255,255,0.1);
        padding: 12px;
        border-radius: 10px;
        font-size: 16px;
    }

    .footer {
        margin-top: 15px;
        font-size: 12px;
        color: #fff;
        opacity: 0.8;
    }

</style>
</head>
<body>

<div class="calculator">
    <h1>Tip Calculator</h1>

    <label for="bill">💵 Enter Bill Amount ($)</label>
    <input type="number" id="bill" placeholder="e.g., 50" min="0">

    <label for="tip">💰 Enter Tip Percentage (%)</label>
    <input type="number" id="tip" placeholder="e.g., 10" min="0">

    <label for="people">👥 Number of People</label>
    <input type="number" id="people" placeholder="e.g., 2" min="1">

    <button onclick="calculateTip()">Calculate Tip</button>

    <div id="result"></div>

    <div class="footer">Developed By: Iram Shahzadi</div>
</div>

<script>
function calculateTip() {
    let bill = parseFloat(document.getElementById("bill").value);
    let tipPercent = parseFloat(document.getElementById("tip").value);
    let people = parseInt(document.getElementById("people").value);

    if(isNaN(bill) || isNaN(tipPercent) || isNaN(people) || bill < 0 || tipPercent < 0 || people <= 0){
        document.getElementById("result").innerHTML = "<span style='color:#fff; font-weight:600;'>Please enter valid values!</span>";
        return;
    }

    let tipAmount = bill * (tipPercent / 100);
    let totalAmount = bill + tipAmount;
    let perPerson = totalAmount / people;

    document.getElementById("result").innerHTML = `
        <strong>Tip Amount:</strong> $${tipAmount.toFixed(2)}<br>
        <strong>Total Amount:</strong> $${totalAmount.toFixed(2)}<br>
        <strong>Per Person:</strong> $${perPerson.toFixed(2)}
    `;
}
</script>

</body>
</html>
